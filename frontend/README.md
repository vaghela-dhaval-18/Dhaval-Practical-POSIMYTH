Follow the staps to run the react application:

- go to the 'frontend' directory path in the terminal.
- run the 'npm install' command in the terminal.
- run the 'npm run dev' command in the terminal.
